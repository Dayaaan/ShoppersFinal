<div class="row mb-5">
    {% for product in products %}
    <div class="col-sm-6 col-lg-4 mb-4" data-aos="fade-up">
        <div class="block-4 text-center border">
        <figure class="block-4-image">
            <a href="{{ path('product_show', {'id': product.id}) }}"><img src="{{ vich_uploader_asset(product.mainImage, 'imageFile') }}" alt="img" class="img-fluid"></a>
        </figure>
        <div class="block-4-text p-4">
            <h3><a href="shop-single.html">{{ product.name }}</a></h3>
            <p class="mb-0">{{ product.description | slice(0,60) }}</p>
            <p class="text-primary font-weight-bold">{{ product.price }} €</p>
            <a href="#" class="buy-now btn btn-sm btn-primary" style="color:white;"><span class="icon icon-shopping-cart"></span></a>
        
        </div>
        </div>
    </div>
    {% endfor %}
div>